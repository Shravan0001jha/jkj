# Agents

The grid of agent cards, the chat view, and the subagent tree.

Planned files:
- `AgentGrid.tsx` — cards for every agent in the selected project
- `AgentChat.tsx` — transcript, composer, approval prompt, actions
- `SubagentChat.tsx` — a subagent's own conversation, one level down
- `NewAgentDialog.tsx` — task, model, workspace mode
