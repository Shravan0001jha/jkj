# Context

Two documents and a live preview of the prompt they assemble into.

Planned files:
- `ContextView.tsx` — the central doc and the project doc side by side
- `AssembledPreview.tsx` — token budget bar and the literal prompt text
