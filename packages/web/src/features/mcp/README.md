# MCP

Installed servers with health, per-project toggles, and the catalog.

Planned files:
- `McpList.tsx` — installed servers, health, restart, enable/disable
- `McpCatalog.tsx` — install a curated server
